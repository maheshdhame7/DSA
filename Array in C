#include<stdio.h>
main()
{
	int i,n,arr[20];
	printf("\n Enter how many element do u want :");
	scanf("%d",&n);
	printf("\n Enter the %d Element :",n);
	printf("\n");
	for(int i=0;i<n;i++)
	{
		scanf("%d",&arr[i]);
	}
	printf("\n Array :");
	for(int i=0;i<n;i++)
	{
		printf("%3d",arr[i]);
	}
	
}
